import type { ComponentDef } from '../types';
export declare function defaultSkeleton(component: ComponentDef, timeoutMs: number): string;
export declare function renderErrorFallback(retryId: string, message?: string): string;
